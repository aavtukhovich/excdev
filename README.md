# Балансы пользователей

Установить зависимости: `composer install` и `npm install`.
Подготовить базу: `php artisan migrate`.
Запуск backend: `php artisan serve`.
Запуск frontend: `npm run dev`, для очереди отдельно `php artisan queue:work`.
